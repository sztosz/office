<h2><%= gettext("Edit case") %></h2>

<%= render "form.html", changeset: @changeset, conn: @conn,
                        clients: @clients,
                        departments: @departments,
                        kinds: @kinds,
                        action: case_path(@conn, :update, @id) %>
