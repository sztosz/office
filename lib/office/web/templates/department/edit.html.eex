<h2>Edit department</h2>

<%= render "form.html", changeset: @changeset,
                        action: court_department_path(@conn, :update, @changeset.data.court_id, @department) %>

<%= link "Back", to: court_department_path(@conn, :index, @changeset.data.court_id) %>
